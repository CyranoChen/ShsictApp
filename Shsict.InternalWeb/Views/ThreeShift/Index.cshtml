@model List<Shsict.InternalWeb.Models.ThreeShift>
@{
    ViewBag.Title = "第一、二、三工班吞吐量";
    Layout = "~/Views/Shared/_LayoutDefault.cshtml";
}
@section Header{   <a id="but_back" data-role="button" data-inline="true" data-rel="back"
    data-transition="slidedown" href="#page1" data-icon="arrow-l"
    class="ui-btn-left but_back" data-iconpos="notext" data-mini="true">Back </a>
    <div class="ui-body ui-body-c" style="padding: 0 10px; margin-bottom: -1px;">
        <form style="float: right">
      @*      <script>
                $(function () {
                    $("#cxp").datepicker();
                });
            </script>
            <p>Date:
                <input type="text" id="cxp" /></p>*@
         
           
            <fieldset data-type="horizontal" data-mini="true">
                   @Html.TextBoxFor(m =>m[0].SHIFTDATE)
           @*    @Html.TextBoxFor(m =>view)*@
                <input type="submit" value="确认"/>
               @* <button data-icon="grid">date</button>*@
            </fieldset>
        </form>
    </div>
}


<h3 class="tit">SHIFT1_PLAN  1</h3>
<table data-role="table" data-mode="reflow" class=" table-stroke">
    <thead>
        <tr>
            <th data-priority="persist">计划 TEU</th>
            <th data-priority="2">实际TEU</th>
            <th data-priority="3">计划完成率</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            @foreach (Shsict.InternalWeb.Models.ThreeShift item in Model)
            {
                if (item.SHIFT.Trim() == "1")
                {   <td>@item.SHIFTPLAN</td>
                <td>@item.SHIFTACTUAL</td>
                <td>@item.SHIFTCOMPLETERATE</td>      
                }
            }
        </tr>
    </tbody>
</table>
<h3 class="tit">SHIFT1_PLAN  2</h3>
<table data-role="table" data-mode="reflow" class=" table-stroke">
    <thead>
        <tr>

            <th data-priority="persist">计划 TEU</th>
            <th data-priority="2">实际TEU</th>
            <th data-priority="3">计划完成率</th>

        </tr>
    </thead>
    <tbody>
        <tr>
            @foreach (Shsict.InternalWeb.Models.ThreeShift item in Model)
            {
                if (item.SHIFT.Trim() == "2")
                {
                <td>@item.SHIFTPLAN</td>
                <td>@item.SHIFTACTUAL</td>
                <td>@item.SHIFTCOMPLETERATE</td>
                }
            }
        </tr>
    </tbody>
</table>
<h3 class="tit">SHIFT1_PLAN  3</h3>
<table data-role="table" data-mode="reflow" class=" table-stroke">
    <thead>
        <tr>
            <th data-priority="persist">计划 TEU</th>
            <th data-priority="2">实际TEU</th>
            <th data-priority="3">计划完成率</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            @foreach (Shsict.InternalWeb.Models.ThreeShift item in Model)
            {
                if (item.SHIFT.Trim() == "3")
                { 
                <td>@item.SHIFTPLAN</td>
                <td>@item.SHIFTACTUAL</td>
                <td>@item.SHIFTCOMPLETERATE</td>
                }
            }
        </tr>
    </tbody>
</table>